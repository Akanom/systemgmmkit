# Artifact 24: Frozen Dynamic-GMM Snapshot

This copied directory is a frozen single-spec JOSS snapshot retained for
submission provenance. It is not an independent or current certification
authority, and its historical exclusion of the constant is superseded.

The current authority is the six-spec registry and unified certificate under
`artifacts/parity/xtabond2/`, which require the complete expected parameter set
and distinguish numerical diagnostic agreement from instrument validity.
